---
import type { HTMLAttributes } from 'astro/types'

export interface Props extends HTMLAttributes<'svg'> {
  name: string
}

const { name, ...rest } = Astro.props
---

<svg {...rest}>
  <use xlink:href={`/sprite.svg#sprite:${name}`}></use>
</svg>
